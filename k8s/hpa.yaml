apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: llm-security-gateway-hpa
  namespace: llm-security-gateway
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: llm-security-gateway-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70